# Session log

Append-only dated journal. Newest entries first. One entry per meaningful
session or task, 3–10 lines. Never rewrite old entries.

To find a period: search by date header, e.g. `rg "## 2026-09" docs/LOG.md`.

Older entries: none archived yet.

## 2026-09-25 — публичный репозиторий, почта

- What: `gh repo create Bonjourchik/shiki-theme --public` по «да» пользователя, `main` запушен; raw-URL
  проверен (200, совпадает с `dist/theme.css`). Почта: сообщения карточками, вкладки разделов на своих
  страницах (`.block.is-own-profile > .b-options-floated`) — таблетками.
- Notes: перед публикацией проверены отслеживаемые файлы — секретов нет, email коммитов noreply.
  При скрытой панели браузера скриншоты чёрные, а CSS-переходы замирают на середине — проверять DOM-замерами.

## 2026-09-25 — оставшиеся группы: подстраницы профиля, календарь, пользователи, настройки

- What: список аниме (кнопка «назад», статусы таблетками), история (интервалы карточками),
  страница достижений (карточки сеткой, франшизы/авторы сеткой), пользователи (карточки, круглые
  аватары, читаемые события), настройки (вкладки таблетками, кнопка файла, тёмный CodeMirror, ползунок).
- Notes: в тяжёлых страницах (профиль, достижения) панель браузера не успевает отрисовать скриншот —
  проверял computed style. Календарь и избранное подошли без правок.

## 2026-09-25 — страницы группами в залогиненной вкладке: тайтл, персонаж/человек, каталог, форум, клубы

- What: тайтл — пары «Связанное/Авторы», «Кадры/Видео», сетка героев/похожего 7 в ряд, планшетная
  раскладка (Инфо и Рейтинг стопкой, постер без sticky), «развернуть» кнопкой, вкладки отзывов переносятся;
  персонаж — секции ролей карточками; человек — «Лучшие работы» сеткой; редактор комментария тёмный;
  каталог — фильтры `.c-ms` (тёмная панель, чипы), свой `select`, пагинация; форум — видимый h1,
  постеры топиков, «новое» и счётчик янтарём; клубы — карточки; профиль на планшете — сетки auto-fill.
- Notes: проверка — в залогиненной вкладке через `localStorage.bjTheme` + `bjApply`, ширины 783 и 1280.
  Сайт часто перебивает через `!important` (`.m0`, `.c-screenshots`), `body[data-locale]` (метка «новое»),
  `.c-filter-select`, `.p-topics header h1 a.reload` — смотреть правила сайта через `document.styleSheets`.

## 2026-09-25 — планшетная ширина главной, отступы

- What: блок «Аниме/Манга/Ранобэ» — сетка из трёх колонок (на 768–1023 сайт растворял колонки
  и теги сливались), «Форум» внизу колонки «Ранобэ» (сайт на планшете делал кнопку absolute 224px),
  строка новости: теги, ниже время и счётчик; свечение ромбов через drop-shadow; лупа, кнопки,
  «Темы дня» 42px; кнопки `.b-link_button` — flex по центру.
- Notes: пользователь проверяет в вкладке панели на ~783px — мои замеры на 1280 не совпадали с его видом.

## 2026-09-25 — главная целиком, зазоры страницы тайтла

- What: `src/30-pages/home.css` — лента «Темы дня» таблетками с круглыми постерами, карточки новостей
  (скруглённое превью с увеличением, затемнение, заголовок), строки-карточки «Контент»/«Темы дня»,
  карточки «Обновлений аниме», блок разделов карточкой, иконка комментариев по центру, «Загрузить ещё».
  Страница тайтла: зазоры между карточками по 16px (у сайта `.c-info-*:last-child` c `!important`).
- Notes: у элемента с `clear` верхний отступ поглощается — зазор задавать нижним отступом плавающего блока.
  Сайт красит ссылки заголовков главной правилом `.p-dashboards-show .v2 .headline.<цвет> a`.

## 2026-09-25 — единый язык мелких элементов, без «карточки в карточке»

- What: по скриншотам пользователя убраны оставшиеся стандартные элементы. Новый
  `src/20-components/elements.css`: заголовки секций без цветных плашек (их давали наши же хелперы
  `.red/.orange/…`), чипы для хэштегов главной и жанров, теги-статусы полупрозрачными «пилюлями»
  цвета типа (`color-mix`), янтарные звёзды рейтинга, вкладки отзывов «таблетками», кнопки соцсетей.
  `.b-link_button` — янтарные; «Добавить в список» — заливка цветом статуса. Страница тайтла:
  «Описание» получило `clear: left` (без этого его карточка обволакивала «Информацию» и «Рейтинг»),
  «Информация»/«Рейтинг» — сеткой.
- Files: src/20-components/{elements,controls,surfaces,misc,blocks}.css.
- Notes: цветные хелперы сайта (`.red`, `.magenta` …) висят и на заголовках, и на ссылках в них.

## 2026-09-25 — редизайн шапки и поверхностей страниц

- What: пользователь указал, что сайт выглядит стандартно, а вместо иконок — пустые квадраты.
  Квадраты: предпросмотр теперь проксирует файлы shikimori.io (шрифт иконок резался CORS).
  Шапка: текстовый логотип «shiki*mori*» с янтарной иконкой, «таблетка» раздела, круглые кнопки,
  поиск-«пилюля», янтарная линия снизу, подменю со скруглениями и выездом (`bj-drop`).
  Страницы: `src/20-components/surfaces.css` — карточки для боковой колонки и секций страницы тайтла,
  заголовок страницы с янтарной линией, крошки-таблетки, постер с тенью, таблицы; графики s0–s3 в янтаре;
  блок входа, теги-статусы.
- Files: tools/preview.mjs, tools/lib/inject.mjs (+тест), tools/audit.js, src/20-components/*, src/40-motion.css.

## 2026-09-25 — шапка и выпадающие меню

- What: по замечанию пользователя дооформлены шапка (серые плашки кнопок, иконки при наведении,
  результаты поиска, мобильная панель поиска) и скрытые по умолчанию состояния, которых не видит
  аудит: выпадающие меню, «добавить в список», раскрытые и инлайн-спойлеры, свёрнутые блоки,
  затенение длинного текста, кнопки закрытия подсказок, строки списка аниме при наведении.
- Files: src/20-components/{menu,controls,blocks,misc}.css, dist/theme.css.
- Notes: `tools/audit.js` проверяет только видимые элементы — выпадающие меню и :hover
  надо проверять принудительно (класс `.active`, вставка разметки). Иконочный шрифт `shikimori`
  в предпросмотре не грузится (CORS) — прямоугольники вместо иконок только локально.

## 2026-09-25 — реализация этапов 1–2 темы Dark Amber

- What: задачи 1–12 плана (субагент на задачу + ревью), затем финальное ревью ветки и доводка
  (уже без субагентов — пользователь попросил быстрее). Итог: 21 тест, `dist/theme.css` ~55 КБ.
- Решения пользователя: `@keyframes` — только transform/opacity/filter/background-position
  (bj-ping, bj-blink переписаны), transition при наведении — любые; «preview» в dev-инструменте не трогать.
- Files: src/**, tools/**, config/field.css, about/about.bb, dist/theme.css, docs/WORKFLOW.md, .gitattributes.
- Commits: 930c3ed … 3d7c704 (ветка dark-amber, слита в main).
- Notes: сайт почти везде специфичнее плана — `header.head h1`, `.p-profiles-show.x1200 … .cc-2a>.c-column`,
  `.achievements .cc-achievements`, `!important` в истории и `.m30`. Переменные сайта переопределены
  в `html[data-color-mode=light]` — перекрываем и там.

## 2026-09-24 — план реализации темы

- What: написан план из 13 задач с готовым кодом сборщика, тестов, предпросмотра и всего CSS.
  Для этого сняты живой DOM профиля, страницы тайтла и комментариев, а также проанализирован
  CSS сайта (светлые фоны и тёмный текст, которые надо перекрыть).
- Files: docs/superpowers/plans/2026-09-24-dark-amber-theme.md, docs/HANDOFF.md, docs/BACKLOG.md.
- Notes: токены — только на `:root`, иначе поле настроек не сможет их переопределить.

## 2026-09-24 — брейнсторм и спецификация темы «Dark Amber»

- What: через визуальные макеты выбраны тёмный минимализм, янтарный акцент, баннер-арт,
  Unbounded + Onest, мобильная версия, карточки «О себе», доставка через GitHub и `@import`.
  Макет профиля прошёл 4 итерации: «скучно» → анимации и превью достижений →
  заливка вместо обводки → баннер выше.
- Files: docs/superpowers/specs/2026-09-24-dark-amber-theme-design.md, design/*, docs/DECISIONS.md, .gitignore.
- Notes: протокольно-относительные `//` url обходят camo — проверено по живому CSS.

## 2026-09-24 — снимок текущего профиля Bonjourchik

- What: изучен живой профиль `shikimori.io/Bonjourchik`: тема Edesign через 5 `@import`,
  своя палитра «Bronze Sunset», фон и обложка с ibb.co, «О себе» пустой.
- Files: docs/CONTEXT.md, docs/HANDOFF.md, docs/BACKLOG.md, AGENTS.md,
  reference/current-user-css-2026-09-24.css.
- Notes: подтверждено вживую, что `@import` с raw.githubusercontent и картинки с ibb работают.

## 2026-09-24 — исследование кастомизации профиля Shikimori

- What: изучены механизм пользовательских стилей и BB-кодов по исходникам
  `shikimori/shikimori` и страницам CSS-club; настроена project memory.
- Files: AGENTS.md, CLAUDE.md, docs/SHIKIMORI.md, docs/CONTEXT.md, docs/BACKLOG.md,
  docs/HANDOFF.md, docs/DECISIONS.md, docs/LOG.md.
- Commits: нет (не git-репозиторий).
- Notes: домен сменился на `shikimori.io`; любое `@media` отключает автообёртку
  `min-width:1024px`; санитайзер вырезает слова `this`/`parent`/`window` и др.
