# Backlog

## Now

- [ ] Task 13 — публикация: бэкап поля, «О себе», репозиторий `Bonjourchik/shiki-theme`, вставка на сайт, живая проверка.

## Next

- [ ] Группы страниц: подстраницы профиля (список, история, избранное, достижения), онгоинги/календарь, пользователи, настройки.
- [ ] Проверить залогиненный вид профиля (кнопки действий, значки статусов, `[animes]` в «О себе»).
- [ ] Этап 3 — подстраницы профиля (список, история, избранное, друзья).

## Later

- [ ] Тосты `.toastify`.

## Tech debt

- Отступ шапки профиля захардкожен (250px / 130px), не следует за `--bj-banner-h`.
- Точка «в сети» всегда зелёная.
- `tools/lib/check.mjs` мягче настоящего санитайзера: голое `charset`, `import` с пробелом,
  `content` с разными кавычками, `data:` вне `url()` не ловятся.
- `tools/preview.mjs` падает целиком при ошибке чтения файлов.
- Цвета литералами вместо токенов: ник `#fff`, `#8a6a3c`, `#f19b93`, `rgba()` меню и сияния.

## Ideas

- Персонаж в углу через `.b-feedback::after` (вне текущего скоупа).
- Вкладки в «О себе» через `[spoiler]`.
- Избранное без одинокого ряда: `repeat(5, minmax(0,1fr))` на десктопе.

## Done

- [x] 2026-09-25 - Группы страниц в залогиненной вкладке: тайтл, персонаж/человек, каталог и фильтры, форум/топик, клубы, коллекции, профиль на планшете.

- [x] 2026-09-25 - Задачи 1–12 плана: сборщик, предпросмотр, этап 1 (весь сайт), этап 2 (профиль), финальное ревью и доводка.
- [x] 2026-09-24 - Брейнсторм и макеты: утверждены дизайн-система и макет профиля (v4), написана спека.
- [x] 2026-09-24 - Снять текущее состояние профиля Bonjourchik (Edesign + «Bronze Sunset»), бэкап своей части CSS в `reference/`.
- [x] 2026-09-24 - Исследовать механику кастомизации профиля Shikimori (`docs/SHIKIMORI.md`).
- [x] 2026-09-24 - Настроить project memory.
