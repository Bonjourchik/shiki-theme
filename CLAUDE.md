@AGENTS.md

## Claude Code

- Follow the shared project instructions in `AGENTS.md`.
- Respond in the user's language unless the user asks otherwise.
- Before risky edits, state affected files, expected result, and risk.
- For documentation updates, patch existing docs instead of regenerating stable files.
- Before writing CSS, re-check `docs/SHIKIMORI.md` (sanitizer words, `@media` wrap, camo).
